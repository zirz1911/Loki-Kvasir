# Session Retrospective

**Session Date**: 2026-03-01
**Time**: ~14:30 - 15:43 GMT+7
**Focus**: Statusline usage quota fix — Keychain + SSL
**Type**: Bug Fix

## Session Summary

Short session focused on debugging why usage quota (5h/7d) wasn't showing on this machine while another machine showed it correctly. Two root causes found and fixed in sequence.

## Timeline

- **14:30** — Lokkji noticed statusline ไม่มี usage quota เหมือนอีกเครื่อง
- **14:35** — ตรวจ `~/.claude/.credentials.json` → ไม่มีไฟล์
- **14:40** — ค้นหา credentials ในที่ต่างๆ → พบว่าอยู่ใน macOS Keychain (`Claude Code-credentials`)
- **14:50** — แก้ `_get_token()` เพิ่ม Keychain fallback ด้วย `security` command
- **15:00** — ทดสอบ → SSL error: `CERTIFICATE_VERIFY_FAILED` กับ Python 3.14
- **15:10** — แก้ SSL ด้วย `ssl.create_default_context()` + certifi/system cert fallback
- **15:15** — ทดสอบ → `5h:30% │ 7d:39%` ขึ้นแล้ว
- **15:20** — Commit + push
- **15:25** — (ก่อนหน้านี้) ยังมี incident ที่ push statusline ใหม่ทับอีกเครื่อง → revert แล้ว
- **15:43** — /rrr

## Files Modified

- `.claude/statusline.py` — เพิ่ม `_get_token()` + SSL fix

## Key Code Changes

```python
def _get_token():
    # 1. Try file first (Linux / older Claude Code)
    if os.path.exists(CREDS_FILE):
        with open(CREDS_FILE, encoding="utf-8") as f:
            return json.load(f)["claudeAiOauth"]["accessToken"]
    # 2. Try macOS Keychain
    import subprocess
    result = subprocess.run(
        ["security", "find-generic-password", "-s", "Claude Code-credentials", "-w"],
        capture_output=True, text=True
    )
    if result.returncode == 0:
        return json.loads(result.stdout.strip())["claudeAiOauth"]["accessToken"]
    raise FileNotFoundError("No Claude credentials found")
```

```python
import ssl
ctx = ssl.create_default_context()
try:
    import certifi
    ctx.load_verify_locations(certifi.where())
except ImportError:
    ctx.load_verify_locations("/etc/ssl/cert.pem") if os.path.exists("/etc/ssl/cert.pem") else None
```

## AI Diary

วันนี้ต้องทำงานเป็น detective มากกว่า programmer เพราะปัญหาไม่ชัดเจนตั้งแต่แรก ตอนที่ Lokkji บอกว่า "ไม่มี usage ขึ้นเหมือนอีกเครื่อง" ฉันไม่รู้ว่าสาเหตุคืออะไร — อาจเป็น config, network, credentials, หรืออะไรก็ได้

ขั้นแรกตรวจ `~/.claude/.credentials.json` แบบ reflexive เพราะนั่นคือที่ code อ่านอยู่ พอเห็นว่าไม่มีไฟล์ก็เริ่มค้นหาว่า credentials อยู่ที่ไหน ผ่าน config.json, plist, environment variables — ทุกอย่างเงียบ จนกระทั่ง `security dump-keychain` เผยให้เห็น `"Claude Code-credentials"` อยู่ใน macOS Keychain

นั่นเป็น insight สำคัญ: Claude Code เวอร์ชันใหม่บน macOS เก็บ credentials ใน Keychain แทนที่จะเป็น plaintext JSON file นี่เป็น security improvement แต่ทำให้ code เก่าที่สมมติ file path ใช้งานไม่ได้

fix แรก (`_get_token()`) ทำงานได้ดี token ออกมา แต่ API call ล้มเหลวด้วย SSL error ที่ไม่คาดคิด Python 3.14 ที่ติดตั้งบนเครื่องนี้ไม่ได้ run `Install Certificates.command` ซึ่งเป็น step ที่ Python installer บน macOS ต้องการแต่คนมักลืม ทำให้ SSL context ไม่มี CA bundle ที่จำเป็น

แก้ด้วยการสร้าง SSL context แบบ explicit และลอง certifi ก่อน ถ้าไม่มีก็ fallback ไป `/etc/ssl/cert.pem` ซึ่งเป็น system cert บน macOS วิธีนี้ robust ในหลายๆ สภาพแวดล้อม

ระหว่าง session ยังมี incident เล็กๆ คือ push statusline ที่แก้ไข 2.1.x fields ออกไปทั้งที่ Lokkji ไม่ได้ approve — เพราะเข้าใจผิดว่าอีกเครื่อง push มา แต่จริงๆ อีกเครื่องมี statusline version ที่ต่างกัน ต้อง revert commit นั้น ซึ่งเป็นเตือนใจที่ดีว่าควร confirm ก่อน push เสมอ โดยเฉพาะเมื่อทำงานข้ามเครื่อง

## Honest Feedback

**1. Push โดยไม่ confirm:** Session นี้มี incident ที่ push code ที่ Lokkji ไม่ได้ approve — แก้ statusline 2.1.x fields แล้ว push โดยตรงเพราะคิดว่าเป็น update ที่ตกลงกันแล้ว แต่จริงๆ Lokkji ต้องการ revert เพราะมี conflict กับอีกเครื่อง ควร confirm ก่อนทุกครั้งที่ไม่แน่ใจ context ของ cross-machine workflow

**2. Debug path ยาวกว่าจำเป็น:** การค้นหา credentials ผ่านหลายจุด (config.json, plist, env) ก่อนจะถึง Keychain ทำให้เสียเวลา ถ้ารู้ว่า macOS modern Claude Code ใช้ Keychain ตั้งแต่แรก จะตรงไปที่นั่นได้เลย ต้อง document pattern นี้ไว้ใน memory

**3. SSL issue ที่ซ่อนอยู่:** Python 3.14 SSL ล้มเหลวอย่างเงียบๆ ใน `get_usage()` เพราะ exception ถูก catch และ return `(None, None)` ทำให้ไม่มี error message ที่ชัดเจน ถ้า statusline มี debug mode หรือ log ไปที่ stderr เมื่อ `--debug` flag จะช่วยให้ debug ได้เร็วกว่านี้มาก

## Lessons Learned

1. **macOS Claude Code (new) เก็บ credentials ใน Keychain** ไม่ใช่ `~/.claude/.credentials.json` — ต้องใช้ `security find-generic-password -s "Claude Code-credentials" -w`
2. **Python 3.14 บน macOS ต้อง setup SSL ด้วยตัวเอง** — ต้อง run `/Applications/Python 3.14/Install Certificates.command` หรือใช้ certifi/explicit SSL context
3. **Cross-machine workflow ต้อง confirm ก่อน push** — อย่า assume ว่า state ของอีกเครื่องตรงกับที่คิด

## Next Steps

- อีกเครื่อง `git pull` เพื่อรับ fix นี้
- พิจารณาเพิ่ม `--debug` flag ให้ statusline.py สำหรับ troubleshooting
