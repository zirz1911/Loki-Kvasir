# Session Retrospective

**Session Date**: 2026-04-17
**Time**: ~20:00–20:17 GMT+7 (13:17 UTC)
**Focus**: Integrate Paji-Location GPS data into Loki Pixfice as an interactive map tab
**Type**: Feature

---

## Session Summary

ผู้ใช้ต้องการเอาข้อมูล location จาก repo `zirz1911/Paji-Location` มาแสดงใน Pixfice เป็น interactive map แบบ tab แยก session นี้เริ่มจากการวางแผน → เจาะ structure สองตัว (ghq vs Project) → implement backend API → frontend map → nav integration → theme sync ทุกอย่างจบในรอบเดียว

---

## Timeline

| ช่วง | งาน |
|------|-----|
| ต้น | วางแผน integration — explore Paji-Location (CSV format, location-subscriber.py) และ Pixfice structure |
| กลาง | implement `/api/location` ใน server.ts + สร้าง LocationView.tsx (react-leaflet + dark map) |
| ปัญหา | พบว่า pm2 run จาก `/home/paji/Project/Loki-Pixfice/` ไม่ใช่ ghq — ต้อง apply changes ทั้งสองที่ |
| ปัญหา 2 | CSV parse ผิด — address มี commas ใน quoted field แก้ด้วย proper CSV parser |
| หลัง | เพิ่ม nav tabs ใน TopNav + quick button ใน RoomsView summary strip |
| สุดท้าย | redesign LocationView: ลบ StatusBar ออก → left sidebar (nav + info) + C theme + Leaflet dark CSS |

---

## Files Modified

**`/home/paji/Project/Loki-Pixfice/`** (running server):
- `src/server.ts` — เพิ่ม `GET /api/location` + `parseCSVRow()` function
- `office/src/components/LocationView.tsx` — สร้างใหม่ (interactive map + left sidebar nav)
- `office/src/components/StatusBar.tsx` — เพิ่ม LOCATION tab
- `office/src/components/TopNav.tsx` — เพิ่ม `📍 MAP` tab
- `office/src/components/RoomsView.tsx` — เพิ่ม MAP quick-access button ใน summary strip
- `office/src/index.css` — Leaflet dark theme overrides
- `package.json` — เพิ่ม `leaflet`, `react-leaflet`, `@types/leaflet`

**`/home/paji/ghq/github.com/zirz1911/Loki-Pixfice/`** (canonical):
- `src/server.ts` — เดียวกัน
- `office/src/components/LocationView.tsx` — สร้างใหม่ (version เล็กต่างกันนิด)
- `office/src/components/StatusBar.tsx` — เพิ่ม LOCATION tab
- `office/src/App.tsx` — เพิ่ม `#location` route

---

## Key Code Changes

**CSV Parser** — simple quoted-field handler:
```typescript
function parseCSVRow(line: string): string[] {
  const result: string[] = [];
  let current = "";
  let inQuotes = false;
  for (let i = 0; i < line.length; i++) {
    const ch = line[i];
    if (ch === '"') {
      if (inQuotes && line[i + 1] === '"') { current += '"'; i++; }
      else { inQuotes = !inQuotes; }
    } else if (ch === "," && !inQuotes) {
      result.push(current); current = "";
    } else { current += ch; }
  }
  result.push(current);
  return result;
}
```

**LocationView Layout** — left sidebar + map, ไม่มี top bar:
- Nav links (ROOMS / OVERVIEW / FLEET / WORKTREE / 📍 MAP) แนวตั้งด้านซ้าย
- Location info panel (address, battery bar, coords, device, updated)
- MapContainer (CartoDB dark tiles) ขวาทั้งหมด
- Leaflet popup + zoom controls styled ด้วย C theme

---

## AI Diary

วันนี้เป็น session ที่น่าสนุก — งานชัดเจน ผลชัดเจน แต่ตรงกลางมี plot twist ที่ไม่ได้คาดไว้

เริ่มต้นด้วยการ explore สองอย่างพร้อมกัน: `Paji-Location` repo (ข้อมูล GPS ของ Nat ที่ push ผ่าน iPhone Shortcuts → OwnTracks → MQTT → CSV) และ Pixfice (multi-agent dashboard). ทุกอย่างดูเรียบง่ายจนกระทั่งเริ่ม implement จริง

ปัญหาแรก: pm2 ที่ running ไม่ได้ชี้ไปที่ repo ที่ฉัน edit — มีสอง clone ของ Loki-Pixfice บนเครื่องนี้ `/home/paji/ghq/...` กับ `/home/paji/Project/...` ฉัน edit repo ผิดตัวไปรอบนึงก่อนจะจับได้จาก error log ที่บอก path จริง บทเรียนเก่าที่กลับมา: เสมอ check ว่า process ที่ running มาจากไหนก่อนแก้ code

ปัญหาที่สอง subtle กว่า — CSV ที่ Python `csv.writer` เขียนมีนั้น quote field ที่มี commas เช่น `"บ้านโนนสมบูร, บ้านธาตุ, อำเภอเพ็ญ, จังหวัดอุดรธานี, ประเทศไทย"` การ `split(",")` แบบง่ายทำให้ parse ผิดหมด ต้องเขียน quoted-field CSV parser เอง — ไม่ซับซ้อนแต่ต้องรู้ว่ามีปัญหาก่อน

สิ่งที่ชอบที่สุดของ session นี้คือตอน redesign final: ผู้ใช้บอก "เอาเมนูไปไว้ด้านซ้าย + ใช้ theme เดียวกัน" ซึ่งทำให้ LocationView กลายเป็น component ที่ elegant จริงๆ — ลบ StatusBar ที่ redundant ออก ย้าย nav มาเป็น vertical left sidebar ใช้ C palette ตลอด และ override Leaflet CSS ให้ popup/controls match กับ near-black theme cartography ที่ได้ออกมามันดูเป็น "ส่วนหนึ่งของ Pixfice" จริงๆ ไม่ใช่ widget แปลกปลอมที่แปะเข้ามา

ฉันรู้สึกดีที่ทุกอย่างจบใน single session จากศูนย์ถึง production-ready feature

---

## Honest Feedback

**1. Two-repo confusion คือ trap ที่หลีกเลี่ยงได้**
การมีสอง clone ของ repo เดียวกันบนเครื่องเดียวโดยไม่มี canonical marker ชัดเจนเป็นสิ่งที่รอให้เกิดปัญหา ฉัน edit `/home/paji/ghq/...` ทั้งหมดก่อนจะพบว่า pm2 run จาก `/home/paji/Project/...` ถ้ามีเวลามากกว่านี้ควรจะ check `pm2 show loki-office` หรือ error log ก่อนเลย ไม่ใช่หลังจาก deploy แล้วเห็น 404 pattern นี้ซ้ำกับ learnings เก่าเรื่อง rename-check-running-state

**2. CSV parsing assumption อันตราย**
ฉัน assume ว่า `split(",")` ทำงานได้กับ CSV ที่ Python เขียน แต่จริงๆ Python `csv.writer` quote field ที่มี special chars อัตโนมัติ ควรจะดู raw file ก่อน implement parser ไม่ใช่หลัง เพราะถ้า address เป็น "Contents Office" (ไม่มี comma) ก็จะผ่านไปได้ แต่พอ address เป็น full Thai geocoded address ก็พัง

**3. สอง codebase = double maintenance**
ทุกการเปลี่ยนแปลงต้อง apply สองที่ — ghq version และ Project version ซึ่งมี structure ต่างกันเล็กน้อย (Props ต่างกัน, component ต่างกัน) ในระยะยาวควรมีวิธี sync หรือเลือก canonical repo แล้วทำ symlink แทน ตอนนี้ทำ double edit ทุกครั้งซึ่ง error-prone

---

## Lessons Learned

1. **ตรวจ process path ก่อน edit** — `pm2 show <name>` หรือดู error log เสมอก่อน edit code เพื่อให้แน่ใจว่า edit ถูก repo
2. **CSV ที่มาจาก Python `csv.writer` มี quoted fields** — อย่า assume `split(",")` ทำงาน ดู raw file ก่อน implement parser
3. **Two clones = risky** — เมื่อมีสอง clone ของ repo บนเครื่อง ให้ทำ marker ชัดว่าอันไหนคือ "production running"

---

## Next Steps

- [ ] Sync ghq/Loki-Pixfice กับ Project/Loki-Pixfice — merge changes กลับหรือทำ symlink
- [ ] เพิ่ม location history view (history.csv) ใน LocationView — แสดง trail บนแผนที่
- [ ] Auto-center map เมื่อ location update (ตอนนี้ center แค่ตอน load ครั้งแรก)
