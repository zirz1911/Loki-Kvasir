# Local machine settings — not for sharing
.claude/settings.local.json
.mcp.json

# Claude Code active state
.claude/todos.json

# Vault — tracked in oracle-vault repo (D:/oracle-vault)
ψ/

# Logs & temp (kept for reference — covered by ψ/ above)
ψ/memory/logs/
ψ/active/
